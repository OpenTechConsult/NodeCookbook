# 10-recipe-communicating-with-socket

A simple program to create a TCP server and a TCP client and allow them to communicate.

# Run

To run the server

> node server.js

To run the client (in a separate shell)

> node client.js
